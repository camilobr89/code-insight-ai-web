export const environment = {
  production: true,
  // Replaced at build time by the CI/CD pipeline (see aws-angular-s3-ci-cd.yml).
  // The placeholder below is substituted with the real API URL via `sed`.
  apiUrl: '__API_URL__',
};
